pipeline {
    agent any
    stages {
        stage('Git') {
            steps {
                checkout scm
            }
        }
        stage('Compile') {
            steps {
                dir('backend') {
                    sh './mvnw compile'
                }
            }
        }
    }
}
