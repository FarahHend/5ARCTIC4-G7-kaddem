eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://http://192.168.33.10:8761/eureka
name:
  value: hend
spring:
  cloud:
    config:
      override-system-properties: false

management:
  tracing:
    sampling:
      probability: 1.0