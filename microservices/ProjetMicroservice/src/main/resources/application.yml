spring:
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: ProjetMicroservice

eureka:
  client:
    service-url:
      defaultZone: http://192.168.33.10:8761
logging:
  level:
    feign:
      client: DEBUG
