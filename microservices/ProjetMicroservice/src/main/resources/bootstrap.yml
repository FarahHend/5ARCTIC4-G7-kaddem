spring:
  config:
    import: optional:configserver:http://192.168.33.10:8887
  application:
    name: ProjetMicroservice
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:default}
  data:
    mongodb:
      username: hend
      password: hend
      host: ${SPRING_DATASOURCE_URL:mongodb}
      port: 27017
      database: ProjetDB
      authentication-database: admin

logging:
  level:
    feign:
      client: DEBUG

eureka:
  instance:
    hostname: 192.168.33.10
  client:
    enabled: false

application:
  config:
    equipe-url: http://192.168.33.10:8000/equipe

server:
  port: 8086
  address: 0.0.0.0
